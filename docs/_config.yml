theme: jekyll-theme-midnight
collections:
  tutorials:
    output: true
    permalink: /:collection/:name
    
defaults:
  -
    scope:
      path: "tutorial" # an empty string here means all files in the project
    values:
      layout: "post"
